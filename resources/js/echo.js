/**
 * Broadcasting has been disabled for shared hosting compatibility.
 * Real-time features now use polling instead of WebSockets.
 */

export const reinitializeEcho = () => {
    // No-op: broadcasting disabled
}

export default null
